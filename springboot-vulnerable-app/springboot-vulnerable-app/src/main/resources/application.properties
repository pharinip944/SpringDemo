# WARNING: Do NOT hardcode secrets in this file. Always use secure environment variables for sensitive values.
# Ensure JWT_SECRET is set securely in your deployment environment and is at least 32 characters long.
# Example for secure setup (do not include in production):
# export JWT_SECRET="your-very-long-random-secret-key"
jwt.secret=${JWT_SECRET}
